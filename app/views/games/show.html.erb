<% if flash[:notice] %>
 <div class="notice"> <%= flash[:notice] %> </div>
<% end %>


<%= form_for @game.guesses.build,
			 url: game_guesses_path(@game) do |f| %>
 	<p> 
  		<strong>Guess:</strong>
  		<%= f.number_field :number %>
  		<%= f.submit %>
	</p>
<% end %>